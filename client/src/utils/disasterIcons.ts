// Disaster icon utility - Currently using Lucide fallbacks since custom assets are empty
// TODO: Replace with actual custom disaster icons when assets are provided

export function getDisasterIcon(disasterType: string): string | null {
  // Custom icons are not available (empty files), using Lucide fallback system
  return null;
}

export function getWeatherAlertIcon(alertType: string): string | null {
  // Custom icons are not available (empty files), using Lucide fallback system
  return null;
}

export function getNasaEonetIcon(eventCategory: string): string | null {
  // Custom icons are not available (empty files), using Lucide fallback system
  return null;
}